import { SessionIdValidatorDirective } from './session-id-validator.directive';

describe('SessionIdValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SessionIdValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
