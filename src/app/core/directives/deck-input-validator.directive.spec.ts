import { DeckInputValidatorDirective } from './deck-input-validator.directive';

describe('DeckInputValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DeckInputValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
